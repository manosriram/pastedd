import express, { Request, Response } from "express";
const router: express.Router = express.Router();

router.post("/create_paste", (req: Request, res: Response) => {});

export { router as create_paste_router };
