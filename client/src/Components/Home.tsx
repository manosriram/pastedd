import React from "react";
import { Box } from "./";

const Home = (props: any) => {
    return (
        <>
            <div className="App">
                <Box />
            </div>
        </>
    );
};

export default Home;
